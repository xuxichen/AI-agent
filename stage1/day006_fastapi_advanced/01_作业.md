# Day 6 · 作业（4 道，约 65 分钟）

> 四道题**全部要真起服务真打**（端口 8771–8774，跟教案的 8761–8768 不冲突）。
> 第 2 题关着教案从零写，第 3 题不碰键盘。
>
> **每道题的「先猜」那一格不填，就别敲第一条 curl。** 今天这四道题的分数全在猜上——
> 打下去谁都会，猜不出来说明那九节是白读的。
>
> 一律在 `stage1/day006_fastapi_advanced/` 下操作，起服务前记得 `cd code/python`。
> 命令一律 `uv run ...`，两个终端：A 起服务、B 打 curl。**第 1、4 题中途要改文件重启**，看题目里的提示。

---

## 第 1 题 · 一个「查订单」接口，三段分开改（14 分钟，:8771）

教案第二节 ④ 你摘过一次 `raise`。这一题反过来：**给你一个真的会 500 的文件，你分三段把它修成前端能用的样子。**

### ① 建文件（原样粘，先别改）

```bash
cd stage1/day006_fastapi_advanced
cat > code/python/hw01_order.py <<'PY'
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()


class OrderOut(BaseModel):
    id: int
    sku: str
    qty: int


orders = [
    {"id": 1, "sku": "A-100", "qty": 2},
    {"id": 2, "sku": "B-7", "qty": 1},
]


@app.get("/orders", response_model=list[OrderOut])
def list_orders():
    return orders


@app.get("/orders/{oid}", response_model=OrderOut)
def read_order(oid: int):
    for it in orders:
        if it["id"] == oid:
            return it


@app.post("/orders", response_model=OrderOut, status_code=201)
def create_order(payload: dict):
    data = {"id": len(orders) + 1, "sku": payload["sku"], "qty": payload["qty"]}
    orders.append(data)
    return data
PY
```

`read_order` 这个函数**结尾没有 `return`**。教案二 ④ 那个 `/strict` 也是这样，但它回 `null [200]`——**这一题不会**。

### ② 先猜六条（**这一格不填，这题等于没做**）

| 条 | 打什么 | 我猜状态码 | 我猜响应体第一个键 | 终端 A 里有没有一屏栈 |
|---|---|---|---|---|
| 1 | GET `/orders` | | | |
| 2 | GET `/orders/1` | | | |
| 3 | GET `/orders/99` | | | |
| 4 | POST `/orders` `{"sku":"C-1","qty":4}` | | | |
| 5 | POST `/orders` `{"sku":"C-1"}`（少 `qty`） | | | |
| 6 | GET `/orders/abc` | | | |

两个提示：第 3 条想一想「函数返回 `None`」和「装饰器上写了 `response_model`」撞在一起会怎样；
第 5 条想一想 `payload["qty"]` 这一行会发生什么（第一节的 `read_key`）。

### ③ 起服务、真跑

```bash
cd code/python
uv run uvicorn hw01_order:app --port 8771
```

```bash
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8771/orders
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8771/orders/1
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8771/orders/99
curl -s -w ' [%{http_code}]\n' -X POST -H 'content-type: application/json' -d '{"sku":"C-1","qty":4}' http://127.0.0.1:8771/orders
curl -s -w ' [%{http_code}]\n' -X POST -H 'content-type: application/json' -d '{"sku":"C-1"}' http://127.0.0.1:8771/orders
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8771/orders/abc
```

终端 A 里数一下刷了几屏栈（今天仍旧只能 eyeball，服务侧输出我没法帮你存）：**应当是 2 屏**，
分别是第 3 条和第 5 条。栈的最后一行一个是 `ResponseValidationError`、一个是 `KeyError`。

**六条真跑：**

```
[{"id":1,"sku":"A-100","qty":2},{"id":2,"sku":"B-7","qty":1}] [200]
{"id":1,"sku":"A-100","qty":2} [200]
Internal Server Error [500]
{"id":3,"sku":"C-1","qty":4} [201]
Internal Server Error [500]
{"detail":[{"type":"int_parsing","loc":["path","oid"],"msg":"Input should be a valid integer, unable to parse string as an integer","input":"abc"}]} [422]
```

三条问答（先自己想，答案里给）：

- 第 3 条和第 5 条**都是 500**，但终端里那两屏栈的**最后一行完全不同**。分别是什么？
- 第 3 条这个 500，教案第八节的 `app.py` 里**已经不存在了**，靠的是哪一句？
- 第 5、6 两条：一个是你的代码坏了，一个是**客户端请求坏了**。哪个该你背？

### ④ 改第一段：只加一行（起 8771 之前先改）

在 `read_order` 底下加一句 `raise`，并且顶部那行 import 要补 `HTTPException`：

```python
from fastapi import FastAPI, HTTPException
```

```python
    raise HTTPException(status_code=404, detail="没有这个订单")
```

改完 `Ctrl-C` 重启，打两条：

```
{"detail":"没有这个订单"} [404]
{"id":1,"sku":"A-100","qty":2} [200]
$ grep -c Traceback = 0
```

**注意那个 `grep -c` 从 2 变成 0**：500 少了两屏栈，404 一行都不多。这就是教案二 ⑤ 说的那条差别。

### ⑤ 改第二段：再加一个处理器

顶部补两行、文件末尾加四行（**写在端点后面就行**，教案四 ⑧ 说过为什么）：

```python
from fastapi import FastAPI, HTTPException, Request
from fastapi.responses import JSONResponse
```

```python
@app.exception_handler(HTTPException)
def on_http(request: Request, exc: HTTPException):
    return JSONResponse(status_code=exc.status_code, content={"code": exc.status_code, "msg": exc.detail})
```

存盘成 `code/python/hw01c_handler.py`（整份文件在答案里，你也可以直接在 ④ 那份上接着改），重启，打四条：

```bash
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8771/orders/99
curl -s -w ' [%{http_code}]\n' -X POST -H 'content-type: application/json' -d '{"sku":"C-1"}' http://127.0.0.1:8771/orders
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8771/orders/abc
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8771/orders/1
```

```
{"code":404,"msg":"没有这个订单"} [404]
Internal Server Error [500]
{"detail":[{"type":"int_parsing","loc":["path","oid"],"msg":"Input should be a valid integer, unable to parse string as an integer","input":"abc"}]} [422]
{"id":1,"sku":"A-100","qty":2} [200]
```

### ⑥ 两个必答

1. 第 2、3 条**没有**被你的处理器改形状。为什么？（一条是 500，一条是 422，理由**不一样**）
2. 现在前端拿到 404 是 `{"code":404,"msg":"..."}`，拿到 422 是 `{"detail":[...]}`。
   要判断「请求出错」，它得写两套代码。你今天能想到几种堵掉这个不一致的办法？各自代价是什么？
   （提示：教案四 ⑥ 那个实验。）

---

## 第 2 题 · 关着教案，从零写一个中间件（16 分钟，:8772）

**这一题不许翻第六节。** 要求只有五条，写不出来就说明第六节 ① 那张表没读懂。

### 要求

1. 一个 `@app.middleware("http")` 的函数，名字随你起，它必须：
   - 进来时 `print` 一行：`[入]` + 方法 + 路径；
   - 出去时 `print` 一行：`[出]` + 状态码 + 耗时（耗时用 Day 3 那把秒表，`round` 保留 4 位）；
   - 给**每一个响应**加一个自定义头 `X-Day: 6`。
2. 三个端点：`GET /ok` 回 `{"done": True}`；`GET /no` 里 `raise HTTPException(403, detail="不给看")`；`GET /die` 里 `return 1 / 0`。
3. **不许注册任何异常处理器。**
4. 中间件那个函数必须是 `async def`，端点全部同步 `def`。
5. 顶部需要的 import，自己写对。

### 先猜：一张三列的表（**这一格不填，这题等于没做**）

| 请求 | `[入]` 会印吗 | `[出]` 会印吗 | 头里有 `X-Day` 吗 | 客户端状态码 |
|---|---|---|---|---|
| `/ok` | | | | |
| `/no` | | | | |
| `/die` | | | | |

**先只猜 `/die` 那一行**，猜完再动键盘。这道题的分全在那三格。

### 真跑

```bash
uv run uvicorn hw02_answer:app --port 8772
```

```bash
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8772/ok
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8772/no
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8772/die
curl -s -D - -o /dev/null http://127.0.0.1:8772/ok | grep -ci x-day
curl -s -D - -o /dev/null http://127.0.0.1:8772/no | grep -ci x-day
curl -s -D - -o /dev/null http://127.0.0.1:8772/die | grep -ci x-day
```

真跑（响应三条 + 计数三条）：

```
{"done":true} [200]
{"detail":"不给看"} [403]
Internal Server Error [500]
```

```
1
1
0
```

### 两个必答

1. `/no` 是 `raise` 出来的 403，为什么 `[出]` 照样印、头照样加？而 `/die` 两样都没有？
2. 如果你的中间件要**在崩溃请求上也能加那个头**，得改哪里？改完客户端会看到什么不同？
   （教案六 ⑤、⑥ 各给了一半答案，这题要你合成一句话。）

---

## 第 3 题 · 一份文件六条请求，只填表不敲键盘（14 分钟，:8773）

先把这份文件**读三遍**，然后**只填表**。这一题一道都不许打——填完再起服务对答案。

```bash
cat > code/python/hw03_pred.py <<'PY'
from fastapi import Depends, FastAPI, HTTPException, Request
from fastapi.responses import JSONResponse

app = FastAPI()


def badge(level: int = 1, tag: str = "free"):
    print("[依赖] badge 在跑")
    if level > 3:
        raise HTTPException(status_code=403, detail="level 太高")
    return {"level": level, "tag": tag}


@app.get("/p1")
def p1(b=Depends(badge)):
    print("[端点] p1")
    return {"拿到的": b}


@app.post("/p2", status_code=201)
def p2(b=Depends(badge)):
    print("[端点] p2")
    raise HTTPException(status_code=409, detail="已经存在了")


@app.exception_handler(HTTPException)
def on_http(request: Request, exc: HTTPException):
    return JSONResponse(
        status_code=exc.status_code,
        content={"code": exc.status_code, "msg": exc.detail},
    )
PY
```

### 六条预测（每格都必须填）

| 条 | 打什么 | 状态码 | 响应体第一个键 | 终端里 `[依赖]` 印吗 | 终端里 `[端点]` 印吗、印哪个 |
|---|---|---|---|---|---|
| 1 | GET `/p1` | | | | |
| 2 | GET `/p1?level=9` | | | | |
| 3 | GET `/p1?level=x` | | | | |
| 4 | POST `/p2` | | | | |
| 5 | GET `/p1?tag=vip` | | | | |
| 6 | GET `/p1?nope=1` | | | | |

**最难的是第 4 条，别跳。** 它同时考了四件事：装饰器上的 `status_code=201`、函数里那句 `print`、函数里那句 `raise`、以及末尾那个处理器。
第 6 条考的是 Day 4 第二节的一条老规则，今天它还在。

### 对答案

```bash
uv run uvicorn hw03_pred:app --port 8773
```

```bash
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8773/p1
curl -s -w ' [%{http_code}]\n' 'http://127.0.0.1:8773/p1?level=9'
curl -s -w ' [%{http_code}]\n' 'http://127.0.0.1:8773/p1?level=x'
curl -s -w ' [%{http_code}]\n' -X POST http://127.0.0.1:8773/p2
curl -s -w ' [%{http_code}]\n' 'http://127.0.0.1:8773/p1?tag=vip'
curl -s -w ' [%{http_code}]\n' 'http://127.0.0.1:8773/p1?nope=1'
```

真跑：

```
{"拿到的":{"level":1,"tag":"free"}} [200]
{"code":403,"msg":"level 太高"} [403]
{"detail":[{"type":"int_parsing","loc":["query","level"],"msg":"Input should be a valid integer, unable to parse string as an integer","input":"x"}]} [422]
{"code":409,"msg":"已经存在了"} [409]
{"拿到的":{"level":1,"tag":"vip"}} [200]
{"拿到的":{"level":1,"tag":"free"}} [200]
```

两个计数（打完六条以后，在终端 A 那个窗口数；也可以像我这样把服务输出重定向到文件再数）：

```
[依赖] badge 在跑  → 5 次
[端点] p1          → 3 次
[端点] p2          → 1 次
```

**这三个数是这题的真正考点**，对上了再往下读：`6 - 5 = 1`，那一条是哪一条？`p1` 打了 5 次为什么只印 3 次？`p2` 明明 `raise` 了为什么那句 `print` 还是跑到了？

---

## 第 4 题 · 主产物：一个「待办」后端，里面埋了三处错（22 分钟，:8774）

教案第八节是把今天的东西拼进昨天的 `app.py`。这一题给你**另一份拼好的**，但它有**三处**不对。

### ① 建文件（**先别改，原样粘**）

```bash
cat > code/python/hw04_todo.py <<'PY'
import time

from fastapi import Depends, FastAPI, HTTPException, Request
from fastapi.responses import JSONResponse
from pydantic import BaseModel, Field

app = FastAPI()


class TodoIn(BaseModel):
    title: str = Field(min_length=3)
    done: bool = False


class TodoOut(BaseModel):
    id: int
    title: str
    done: bool


todos = [
    {"id": 1, "title": "写作业", "done": False},
    {"id": 2, "title": "倒垃圾", "done": True},
]


def todo_page(page: int = 1, size: int = 2):
    if page < 1:
        raise HTTPException(status_code=400, detail="页码从 1 开始")
    return {"skip": (page - 1) * size, "size": size}


@app.middleware("http")
async def timer(request: Request, call_next):
    start = time.perf_counter()
    print("[入]", request.url.path)
    response = await call_next(request)
    print("[出]", response.status_code, round(time.perf_counter() - start, 4))
    return response


@app.exception_handler(KeyError)
def on_bad(request: Request, exc):
    return JSONResponse(status_code=500, content={"code": 500, "msg": "出错了"})


@app.get("/todos", response_model=list[TodoOut])
def list_todos(pg=Depends(todo_page)):
    found = []
    n = 0
    for t in todos:
        if n >= pg["skip"]:
            if len(found) < pg["size"]:
                found.append(t)
        n = n + 1
    return found


@app.get("/todos/{tid}", response_model=TodoOut)
def read_todo(tid: int):
    for t in todos:
        if t["id"] == tid:
            return t
    return {"error": "没有这条待办"}


@app.get("/boom")
def boom():
    return 1 / 0


@app.post("/todos", response_model=TodoOut)
def create_todo(payload: TodoIn):
    data = payload.model_dump()
    data["id"] = len(todos) + 1
    todos.append(data)
    return data
PY
```

### ② 先猜八条（把状态码填进表，**这一格不填不许起服务**）

| 条 | 打什么 | 状态码 | 有没有一屏栈 |
|---|---|---|---|
| 1 | GET `/todos` | | |
| 2 | GET `/todos?page=2&size=2` | | |
| 3 | GET `/todos/1` | | |
| 4 | GET `/todos/99` | | |
| 5 | GET `/boom` | | |
| 6 | GET `/todos?page=0` | | |
| 7 | POST `/todos` `{"title":"买牛奶"}` | | |
| 8 | POST `/todos` `{"title":"买"}` | | |

第 2 条是纯算题（`skip` 是多少、够不够取）。剩下的自己判断，然后**特别注意第 4、5、7 三条**——那三条里就埋着三处错。

### ③ 真跑八条，再数三个计数

```bash
uv run uvicorn hw04_todo:app --port 8774
```

```bash
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8774/todos
curl -s -w ' [%{http_code}]\n' 'http://127.0.0.1:8774/todos?page=2&size=2'
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8774/todos/1
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8774/todos/99
curl -s -w ' [%{http_code}]\n' http://127.0.0.1:8774/boom
curl -s -w ' [%{http_code}]\n' 'http://127.0.0.1:8774/todos?page=0'
curl -s -w ' [%{http_code}]\n' -X POST -H 'content-type: application/json' -d '{"title":"买牛奶"}' http://127.0.0.1:8774/todos
curl -s -w ' [%{http_code}]\n' -X POST -H 'content-type: application/json' -d '{"title":"买"}' http://127.0.0.1:8774/todos
```

真跑：

```
[{"id":1,"title":"写作业","done":false},{"id":2,"title":"倒垃圾","done":true}] [200]
[] [200]
{"id":1,"title":"写作业","done":false} [200]
Internal Server Error [500]
Internal Server Error [500]
{"detail":"页码从 1 开始"} [400]
{"id":3,"title":"买牛奶","done":false} [200]
{"detail":[{"type":"string_too_short","loc":["body","title"],"msg":"String should have at least 3 characters","input":"买","ctx":{"min_length":3}}]} [422]
```

八条打完之后，终端 A 里数这三个（我把它写成命令，你对自己的数）：

```
[入] 出现 8 次
[出] 出现 6 次
Traceback 出现 2 次
```

**`8 - 6 = 2`。少的那两次 `[出]` 是哪两条请求？** 这一问就是第六节那三行现象的迁移版。

### ④ 修，但**每处只许改一行**（三处一共三行）

三处错的特征：

- 第 (1) 处：`read_todo` 里有一行**属于昨天的写法**，今天的正确写法只要把那一行换掉，404 就出来了。
- 第 (2) 处：装饰器里那个**类型**今天这台机器上一个都不会发生。改一个词，`/boom` 就变成你的形状。
- 第 (3) 处：一个**参数**没写，新增成功还是 200。

**先别急着改第 (2) 处。** 改完第 (1) 处以前，先只做第 (2) 处那一行（`KeyError` → `Exception`），重启，重打第 4、5 两条，你会看到两个 500 **变成了同一个形状**——
这是这一题最值得撞一次的地方，答案里我给了那次跑出来的终端原文。

### ⑤ 三条问答（这才是这一题真正要交的东西）

1. 三处各改哪一行？改完第 4、5、7 条分别变成什么？
2. 第 (2) 处改成 `Exception` 之后，`/boom` 被你的处理器接管了，但**中间件里的 `[出]` 依然不印**（少的那一次从 2 变成 1）。
   这说明处理器和你自己的中间件，谁在更外面？
3. 第 6 条 `/todos?page=0` 回的是 `{"detail": ...}` 而不是 `{"code": ..., "msg": ...}`，
   而你明明在 ④ 里把处理器注册成了「`Exception` 这个范围」。为什么没接管它？

---

## 排错对照表（每一条都是我今天在这台机器上撞过的）

| 症状 | 大概率原因 |
|---|---|
| 第 2 题写完起不来：`NameError: name 'Request' is not defined` | 顶部 import 少了 `Request`，但你在中间件签名里写了它 |
| 第 2 题 `[出]` 三条全不印 | 把 `print` 写在了 `return response` **后面**（那行永远到不了） |
| 第 2 题头里三条都有 `X-Day`，连 `/die` 也有 | 你在 `except` 里新建了一个响应并把头加上了——那是教案六 ⑥ 实验二那条路，代价是终端那屏栈没了 |
| 第 4 题改完第 (1) 处，`/todos/99` 变成 `{"detail":...}` 但你要的是 `{"code":...}` | 你只注册了 `Exception`（或 `KeyError`），没注册 `HTTPException` |
| 第 4 题 POST 一直 422，说 `model_attributes_type` | `-H 'content-type: application/json'` 那一段漏了，body 变成了字符串（Day 5 第一条老账） |
| 任何一题 422 的 `loc` 是 `["query", ...]` 你却以为是 body | 那是**依赖函数的参数**，它是查询参数（教案七 ⑤ 那份 `parameters`） |

## 今天四道题的共同判据

**二十行 curl 结果（第 1 题 6 行、第 2 题 6 行、第 4 题 8 行）全对上，再加第 3 题那张六行预测表全对，才算过。**
时间不够就先保住第 2 题和第 4 题——它们合起来就是大纲那句「给接口加上日志中间件和全局异常处理」。
第 3 题不敲键盘，是唯一一道「在地铁上也能做」的题。

## 交作业

```bash
cd stage1/day006_fastapi_advanced
ls code/python/
```

四道题的文件（`hw01_*.py`、`hw02_*.py`、`hw03_pred.py`、`hw04_*.py`）**留不留都行**：
这一卷的真相在那三份 md 里，题目和答案都给了整份 `cat >` 块，**重贴一次就能从零重建**（教案开头就写了：就算 `app.py` 不在了，第八节 ① 也给了整块重贴的版本）。
**只有 `code/python/app.py` 不许删**——明天 Day 7 第一节要 `cp` 它，而且只 `cp` 它。
